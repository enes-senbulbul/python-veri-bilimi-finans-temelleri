[project]
name = "p111-ornek-proje"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Enes Şenbülbül", email = "enessenbulbul1@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
    "requests>=2.34.2",
]

[project.scripts]
p111-ornek-proje = "p111_ornek_proje:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
